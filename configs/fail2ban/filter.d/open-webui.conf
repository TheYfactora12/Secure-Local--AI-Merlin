# home-ai-elite — Fail2ban filter for Open WebUI (v0.7)
# Matches failed login attempts in Nginx access logs
# Pattern: POST /api/auth/signin returning 401 or 403

[Definition]
failregex = ^<HOST> .* "POST /api/auth/signin HTTP.*" (401|403) .*$
ignoreregex =
